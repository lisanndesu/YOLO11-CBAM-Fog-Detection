# Ultralytics YOLO11n + 2层CBAM + FogGate（P4融合后插入）
# Parameters
nc: 12  # 改为你的杂草类别数
scales:
  n: [0.50, 0.25, 1024]
  s: [0.50, 0.50, 1024]
  m: [0.50, 1.00, 512]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.50, 512]

# YOLO11n backbone（0-10，无改动）
backbone:
  - [-1, 1, Conv, [64, 3, 2]] # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]] # 2
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]] # 4
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  - [-1, 2, C3k2, [512, True]] # 6
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 2, C3k2, [1024, True]] # 8
  - [-1, 1, SPPF, [1024, 5]] # 9
  - [-1, 2, C2PSA, [1024]] # 10

# Head + FogGate + 2层CBAM（11-27）
head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 11
  - [[-1, 6], 1, Concat, [1]] # 12
  - [-1, 1, FogGate, []] # 🔴 新增 FogGate（P4融合后）→ 13
  - [-1, 2, C3k2, [512, False]] # 14 (P4融合层)

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 15
  - [[-1, 4], 1, Concat, [1]] # 16
  - [-1, 2, C3k2, [256, False]] # 17 (P3原始层)
  - [-1, 1, CBAM, [64]] # 🔴 P3-CBAM → 18

  - [-1, 1, Conv, [256, 3, 2]] # 19
  - [[-1, 14], 1, Concat, [1]] # 20
  - [-1, 2, C3k2, [512, False]] # 21 (P4原始层)
  - [-1, 1, CBAM, [128]] # 🔴 P4-CBAM → 22

  - [-1, 1, Conv, [512, 3, 2]] # 23
  - [[-1, 10], 1, Concat, [1]] # 24
  - [-1, 2, C3k2, [1024, True]] # 25 (P5原始层)

  # Detect 对应新行号 [18,22,25]
  - [[18, 22, 25], 1, Detect, [nc]] # Detect(P3, P4, P5)