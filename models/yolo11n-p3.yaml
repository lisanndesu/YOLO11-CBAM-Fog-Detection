# YOLO11n-p3: 超轻量学生，只检测小目标层
nc: 12  # 你的类别数
scales:
  n: [0.50, 0.25, 1024]  # depth=0.5, width=0.25

# Backbone 同 yolo11n（不变）
backbone:
  - [-1, 1, Conv, [64, 3, 2]]
  - [-1, 1, Conv, [128, 3, 2]]
  - [-1, 2, C3k2, [256, False, 0.25]]
  - [-1, 1, Conv, [256, 3, 2]]
  - [-1, 2, C3k2, [512, False, 0.25]]
  - [-1, 1, Conv, [512, 3, 2]]
  - [-1, 2, C3k2, [512, True]]
  - [-1, 1, Conv, [1024, 3, 2]]
  - [-1, 2, C3k2, [1024, True]]
  - [-1, 1, SPPF, [1024, 5]]
  - [-1, 2, C2PSA, [1024]]

# Head: 只保留 P3，砍掉 P4/P5
head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 6], 1, Concat, [1]]
  - [-1, 2, C3k2, [512, False]]
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 4], 1, Concat, [1]]
  - [-1, 2, C3k2, [256, False]]  # P3 输出层（行号 16）

  # 🔴 关键：只检测 P3，砍掉 P4/P5 的 Conv + Concat + C3k2
  - [[16], 1, Detect, [nc]]  # 只读 P3