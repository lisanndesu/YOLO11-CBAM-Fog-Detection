# Ultralytics YOLO11n + 2层CBAM（修正Detect行号）
# Parameters
nc: 80 # 改为你的杂草类别数（如12）
scales:
  n: [0.50, 0.25, 1024]
  s: [0.50, 0.50, 1024]
  m: [0.50, 1.00, 512]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.50, 512]

# YOLO11n backbone（行号0-10，无修改）
backbone:
  - [-1, 1, Conv, [64, 3, 2]] # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]] # 2
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]] # 4
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  - [-1, 2, C3k2, [512, True]] # 6
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 2, C3k2, [1024, True]] # 8
  - [-1, 1, SPPF, [1024, 5]] # 9
  - [-1, 2, C2PSA, [1024]] # 10

# YOLO11n head + 2层CBAM（行号11-25）
head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 11
  - [[-1, 6], 1, Concat, [1]] # cat backbone P4 → 12
  - [-1, 2, C3k2, [512, False]] # 13 (P4融合层)

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 14
  - [[-1, 4], 1, Concat, [1]] # cat backbone P3 → 15
  - [-1, 2, C3k2, [256, False]] # 16 (P3原始层)
  - [-1, 1, CBAM, [64]] # 🔴 新增CBAM → 17（P3增强层）

  - [-1, 1, Conv, [256, 3, 2]] # 18
  - [[-1, 13], 1, Concat, [1]] # cat head P4 → 19
  - [-1, 2, C3k2, [512, False]] # 20 (P4原始层)
  - [-1, 1, CBAM, [128]] # 🔴 新增CBAM → 21（P4增强层）

  - [-1, 1, Conv, [512, 3, 2]] # 22
  - [[-1, 10], 1, Concat, [1]] # cat head P5 → 23
  - [-1, 2, C3k2, [1024, True]] # 24 (P5原始层，无CBAM)

  # 🔴 关键修正：Detect读取[17,21,24]（P3/P4加CBAM，P5无）
  - [[17, 21, 24], 1, Detect, [nc]] # Detect(P3, P4, P5)